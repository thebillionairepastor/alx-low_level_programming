#include "main.h"
#include "_putchar"

/**
 * _isdigit -  a function that checks for c digit 
 * @c: input
 * Return: 0 if c is a digit, 0 otherwise
 */
int _isdigit(int c)
{
if (c >= 48) && (c <= 57)
{
return (1);
}	
else
{
return (0);
}
}
