#include "main.h"

/**
 * print_times_table - Prints the times table of the input,
 *                     starting with 0.
 * @n: The value of the times table to be printed.
 */
int  _isalpha(int c)
	if (((c >= 'a' ) && (c<= 'z')) || ((c >= 'A') && (c <= 'z')))
		return (1);
	else 	
		return (0);
	
